- name: install packages using loops
  hosts: local
  connection: local
  become: yes
  tasks:
    - name: install
      ansible.builtin.package:
        name: "{{item}}"
        state: latest
        loop:
          - nginx
          - mysql
          - zip
